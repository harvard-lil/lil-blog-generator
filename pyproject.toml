[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = ">=3.12,<4.0"
flask = "^3.1.3"
gunicorn = "^23.0.0"
requests = "^2.32.0"
pyyaml = "^6.0.2"
unidecode = "^1.4.0"
python-dotenv = "^1.0.1"
urllib3 = ">=2.6.3,<3.0.0"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
